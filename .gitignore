# Personal debug export (may contain prompt/reply data)
ME-rewrite-debug.json

# Local working artifacts — review ledgers, plans, task state. Internal notes
# with local paths in them, not part of the shipped extension. Keep on disk,
# keep out of the public repo.
review/
docs/superpowers/

# OS / editor cruft
.DS_Store
Thumbs.db
node_modules/
